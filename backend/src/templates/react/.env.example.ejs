<% const apiService = dependencies.find(d => ['node', 'python-flask', 'python-fastapi'].includes(d.type)); %>
<% if (apiService) { %>
REACT_APP_API_URL=http://localhost:<%= apiService.config?.port || 5000 %>
<% } %>
